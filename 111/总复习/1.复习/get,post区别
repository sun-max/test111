get 与 post是http协议的两种数据请求方式，本质上属于tcp链接，并无差异，导致他们的差异的是：
http的规定与浏览器的限制，导致他们在应用过程中出现不同
1.get 请求可以主动被浏览器缓存，post请求无法缓存，除非是手动去设置
2.get请求的在浏览器回退时候是无害的，而post会再次请求
3.get请求通过url传递，post直接放在请求头中。
4.get只能通过url编码，post支持多种编码方式
5.get请求在浏览器中有长度限制，post不存在
6.get请求说产生的url地址可以直接被浏览器加入到书签，post不可以
7.最重要的区别：get产生一个tcp数据包，而post产生两个
对于post请求只需要一次就好了，服务器返回200
对于post请求，浏览器先发送header，服务器返回100，浏览器再发送data，浏览器返回响应200.
